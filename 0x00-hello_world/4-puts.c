#include <stdio.h>
#include <string.h>
/**
  *main -entry point
  *
  *return: always 0
  */
int main(void)
{
	char str[55];

	strcpy(str,"\"Programming is like building a multilingual puzzle");
        puts(str);
	return (0);
}
